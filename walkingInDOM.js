const cl = console.log

let obj = {
    shihab : 10,
    radia : 12,
    tahsin : 8
}

cl(`type of document ${typeof document}`)
cl(`document = ${document.documentElement}`)
cl(document.documentElement)
cl(document.head)
cl(document.body)
cl(`type of obj ${typeof obj}`)
cl(obj.shihab)
cl(obj["shihab"])